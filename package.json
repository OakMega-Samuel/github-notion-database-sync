{
  "name": "github-notion-database-sync",
  "version": "1.0.0",
  "private": true,
  "description": "One-way sync of spec .md files in this repo into a Notion database.",
  "type": "commonjs",
  "scripts": {
    "sync": "node scripts/sync.js"
  },
  "dependencies": {
    "@notionhq/client": "^5.26.0"
  }
}
